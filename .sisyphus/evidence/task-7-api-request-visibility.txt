Task 7: 收紧 ApiRequest 字段可见性（pub → pub(crate)）
==================================================
日期: 2026-02-26
状态: ✅ 完成

变更文件:
1. crates/openlark-core/src/api/mod.rs (行 95-102)
   - method: pub → pub(crate)
   - url: pub → pub(crate)
   - headers: pub → pub(crate)
   - query: pub → pub(crate)
   - body: pub → pub(crate)
   - file: pub → pub(crate)
   - timeout: pub → pub(crate)
   - _phantom: pub → pub(crate)

2. crates/openlark-auth/src/auth/authen/v1/user_info/get.rs (行 77-88)
   - 修复跨 crate 直接字段访问：
     - api_request.headers.insert(...) → api_request.header(...)
     - api_request.query.insert(...) → api_request.query(...)
   - 使用 builder 方法替代直接字段访问

验证结果:
- [x] cargo check --workspace 通过（零新错误）
- [x] grep 确认无裸 pub 字段残留
- [x] grep 确认 8 个字段均为 pub(crate)
- [x] getter 方法已存在（method(), api_path(), file(), to_bytes() 等）
- [x] 构造器方法（get/post/put/patch/delete）未修改
