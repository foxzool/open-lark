# Task 3: 删除空/废弃 features 证据

## 删除的 Features

以下空/废弃 features 已从 `crates/openlark-client/Cargo.toml` 中删除：

### 空 Features (无实际功能)
- calendar = []      # 日历会议
- admin = []        # 管理功能
- approval = []     # 审批流程
- helpdesk = []     # 帮助台
- mail = []         # 邮件服务
- application = []  # 应用服务

### 废弃 Legacy Features
- collab = []       # 协作功能
- people = []      # 人员管理
- hire = []         # 招聘管理

## 验证结果

### 1. Cargo.toml 中已无这些 Features
```
$ grep -rn '"calendar"\|"admin"\|"approval"\|"helpdesk"\|"mail"\|"application"\|"collab"\|"people"\|"hire"' crates/openlark-client/Cargo.toml
No matches found in Cargo.toml
```

### 2. 编译验证通过
```
$ cargo check -p openlark-client --all-features
    Checking openlark-client v0.15.0-dev
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 2.24s
```

### 3. 保留的 Features (有实际功能)
- auth
- communication
- docs
- security
- hr
- ai
- bitable
- cardkit
- meeting
- client (空但被引用)
- websocket

## 注意

源文件中仍有对已删除 features 的 cfg 引用 (utils.rs, registry/feature_flags.rs, registry/service_factory.rs)，但由于这些 features 已从 Cargo.toml 删除，相关代码在编译时被跳过。
