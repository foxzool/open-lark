# Task 8 - RC Verification Matrix

## Build Verification Commands

### Default Features
```bash
cargo build
```
**Expected**: Success with default `core-services` feature

### No Default Features
```bash
cargo build --no-default-features
```
**Expected**: Success (should still compile with just core)

### Core Services
```bash
cargo build --features "core-services"
```
**Expected**: Success with auth, communication, docs, workflow

### Individual Features
```bash
cargo build --features auth
cargo build --features communication
cargo build --features docs
cargo build --features hr
cargo build --features workflow
cargo build --features meeting
cargo build --features ai
# etc.
```
**Expected**: All succeed

### All Features
```bash
cargo build --all-features
```
**Expected**: Success with entire workspace

## Test Verification Commands

```bash
cargo test --lib --all-features
```
**Expected**: All tests pass

## Documentation Verification

```bash
cargo doc --no-deps --all-features --lib
```
**Expected**: No warnings, docs generated

## Release Checklist

### Pre-Release
- [ ] Version bumped in `Cargo.toml` (workspace.version)
- [ ] CHANGELOG.md updated with version entry
- [ ] All evidence files reviewed
- [ ] Git tag created: `git tag v0.15.0-rc.X`

### Release Process
- [ ] Push tag: `git push origin v0.15.0-rc.X`
- [ ] Verify GitHub Actions workflow triggers
- [ ] Confirm `validate` job passes
- [ ] Confirm `github-release` job creates release
- [ ] Confirm `crates-io-release` job is SKIPPED (RC only)

### Post-Release Verification
- [ ] GitHub Release page shows new RC
- [ ] Release notes populated from CHANGELOG
- [ ] No crates.io publish attempted (check Actions logs)

## Feature Matrix Status

| Feature | Compiles | Tested | Documented |
|---------|----------|--------|------------|
| core-services | ✅ | ✅ | ✅ |
| auth | ✅ | ✅ | ✅ |
| communication | ✅ | ✅ | ✅ |
| docs | ✅ | ✅ | ✅ |
| hr | ✅ | ✅ | ✅ |
| workflow | ✅ | ✅ | ✅ |
| meeting | ✅ | ✅ | ✅ |
| ai | ✅ | ✅ | ✅ |
| security | ✅ | ✅ | ✅ |
| cardkit | ⚠️ | ⚠️ | ⚠️ |
| webhook | ✅ | ✅ | ✅ |

**Note**: cardkit has known issue (missing export) tracked in Task 6
