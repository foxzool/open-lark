# 注意：此文件仅为示例模板，请复制为 `.env` 并填写真实值。
# 切勿将包含真实凭据的 `.env` 提交到版本库。

# 必填：应用凭据
APP_ID=your_app_id
APP_SECRET=your_app_secret

# 常用可选：运行部分示例所需
# 某些示例（如云文档/下载/表格等）需要用户访问令牌
USER_ACCESS_TOKEN=your_user_access_token

# 发送消息或调用 IM 相关示例时可能用到
RECEIVE_ID=target_user_open_id_or_chat_id
CHAT_ID=

# 云文档/表格/多维表格等示例常用资源标识
SPREADSHEET_TOKEN=
TABLE_ID=
FILE_TOKEN=
FOLDER_TOKEN=

# 日志级别（可选）
RUST_LOG=info

# 其它可能被部分示例使用的变量（按需填写）
APP_TOKEN=
USER_ID=
TENANT_KEY=

