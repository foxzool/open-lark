# openlark-webhook

飞书开放平台 Webhook（自定义机器人）API 模块。

本 crate 的 API 实现严格按 `api_list_export.csv` 的命名规范组织：

`src/bizTag/meta.project/meta.version/meta.resource/meta.name.rs`

当前覆盖 `bizTag=webhook`（自定义机器人、Webhook 事件处理接口）。
