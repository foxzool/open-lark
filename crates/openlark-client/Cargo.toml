[package]
name = "openlark-client"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Client module for OpenLark SDK - provides unified client interfaces and service aggregation"

[dependencies]
# Core dependencies
openlark-core = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
async-trait = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }

# Optional service dependencies (conditionally included)
openlark-docs = { workspace = true, optional = true }
openlark-hr = { workspace = true, optional = true }
openlark-ai = { workspace = true, optional = true }
openlark-auth = { workspace = true, optional = true }

[features]
default = []

# Core client feature
client = []

# Feature flags for service inclusion
# 与新的 openlark-docs feature 分类同步
docs = ["openlark-docs/ccm-doc"]
sheets = ["openlark-docs/ccm-sheets"]
bitable = ["openlark-docs/bitable"]
wiki = ["openlark-docs/ccm-wiki"]
drive = ["openlark-docs/ccm-drive"]
ccm = ["openlark-docs/ccm"]
cloud-docs = ["openlark-docs/cloud-docs"]

# 新的标准化特性名
ccm-doc = ["openlark-docs/ccm-doc"]
ccm-sheets = ["openlark-docs/ccm-sheets"]
ccm-wiki = ["openlark-docs/ccm-wiki"]
ccm-drive = ["openlark-docs/ccm-drive"]

hr = ["openlark-hr"]
ai = ["openlark-ai"]
auth = ["openlark-auth"]

# Convenience features
all-services = ["docs", "sheets", "bitable", "wiki", "drive", "ccm", "hr", "ai", "auth"]
base-services = ["docs", "sheets", "auth"]

# Complete client features for easy selection
client-docs = ["client", "docs"]
client-hr = ["client", "hr"]
client-ai = ["client", "ai"]
client-auth = ["client", "auth"]
client-all = ["client", "all-services"]
