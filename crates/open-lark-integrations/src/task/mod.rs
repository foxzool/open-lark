//! $dir 模块
//!
//! 此模块目前为占位符，将来实现相关的集成功能。

/// 服务
pub struct Service {
    // 占位符
}

impl Service {
    pub fn new(_config: openlark_core::config::Config) -> Self {
        Self {}
    }
}

pub mod prelude {
    pub use crate::prelude::*;
}
