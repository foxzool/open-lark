# OpenLark 功能选择指南

## 概述

OpenLark 采用分层功能设计，允许您根据具体需求选择合适的功能模块。这种设计既能保持极简的使用体验，又能提供按需定制的能力。

## 🎯 快速开始

如果您是新用户或不确定需要什么功能，建议从默认配置开始：

```toml
[dependencies]
open-lark = "0.15"
```

默认启用 `docs-collaboration`，包含 IM 消息、文档协作和认证功能，满足 60% 用户的文档协作需求。

## 📦 功能分层体系

### 1. 文档协作层（默认启用）
**Feature**: `docs-collaboration`

**包含模块**：
- IM 消息发送和接收
- 联系人管理
- 群组管理
- 云文档协作（文档、表格、知识库）
- 完整认证系统

**适用场景**：文档协作应用、企业办公平台、知识管理系统

```toml
[dependencies]
open-lark = "0.15"  # 默认配置
```

### 2. IM通讯层
**Feature**: `communication-core`

**包含模块**：
- IM 消息发送和接收
- 联系人管理
- 群组管理

**适用场景**：聊天机器人、消息通知系统、纯通讯工具

```toml
[dependencies]
open-lark = { version = "0.15", features = ["communication-core"] }
```

### 3. 专业层
**Feature**: `professional-suite`

**包含模块**：
- 文档协作层所有功能
- 企业级协作特性

**适用场景**：专业协作平台、团队协作工具

```toml
[dependencies]
open-lark = { version = "0.15", features = ["professional-suite"] }
```

### 4. 企业层
**Feature**: `enterprise-suite`

**包含模块**：
- 专业层所有功能
- 人力资源管理（考勤、招聘、CoreHR）
- AI 智能功能

**适用场景**：大型企业应用、人力资源系统、智能办公平台

```toml
[dependencies]
open-lark = { version = "0.15", features = ["enterprise-suite"] }
```

### 4. 完整层
**Feature**: `full-suite`

**包含模块**：
- 所有可用功能
- 管理员工能
- 审批流程
- 所有扩展模块

**适用场景**：功能完备的企业级应用、飞书功能替代方案

```toml
[dependencies]
open-lark = { version = "0.15", features = ["full-suite"] }
```

## 🎨 场景化功能组合

根据您的具体业务场景选择：

### IM 通讯应用
```toml
[dependencies]
open-lark = { version = "0.15", features = ["im-suite"] }
```

### 文档协作系统
```toml
[dependencies]
open-lark = { version = "0.15", features = ["docs-suite"] }
```

### 人力资源系统
```toml
[dependencies]
open-lark = { version = "0.15", features = ["hr-suite"] }
```

### AI 智能应用
```toml
[dependencies]
open-lark = { version = "0.15", features = ["ai-suite"] }
```

### 通讯 + 文档组合
```toml
[dependencies]
open-lark = { version = "0.15", features = ["communication-core", "docs-suite"] }
```

## 🔧 技术支持功能

### WebSocket 实时通信
```toml
[dependencies]
open-lark = { version = "0.15", features = ["websocket"] }
```

### OpenTelemetry 可观测性
```toml
[dependencies]
open-lark = { version = "0.15", features = ["otel"] }
```

### 完整技术栈
```toml
[dependencies]
open-lark = { version = "0.15", features = ["professional-suite", "websocket", "otel"] }
```

## 📋 功能对照表

| Feature | 包含模块 | 编译大小 | 适用场景 |
|---------|---------|---------|---------|
| `docs-collaboration`（默认） | IM + 文档 + 认证 | 中等 | 文档协作应用 |
| `communication-core` | IM + 联系人 | 小 | 纯通讯应用 |
| `professional-suite` | 企业协作功能 | 中等 | 专业协作平台 |
| `enterprise-suite` | 专业 + HR + AI | 大 | 人力资源系统 |
| `full-suite` | 所有功能 | 最大 | 完整企业应用 |

## 💡 使用建议

### 新手用户
```toml
# 从默认开始，简单易用
open-lark = "0.15"
```

### 中级应用
```toml
# 选择特定场景组合
open-lark = { version = "0.15", features = ["professional-suite"] }
```

### 高级用户
```toml
# 精确组合所需功能
open-lark = { version = "0.15", features = [
    "communication-core",
    "docs-suite",
    "websocket",
    "otel"
] }
```

### 性能优化
选择最少必需的功能可以：
- 减少编译时间
- 降低二进制文件大小
- 减少运行时内存占用

### 开发阶段
- **开发时**：使用 `full-suite` 便于测试
- **测试时**：使用具体的功能组合
- **生产时**：使用最小必需功能

## 🔄 功能升级路径

您可以逐步升级功能组合：

```toml
# 阶段1：基础通讯
open-lark = "0.15"

# 阶段2：增加文档功能
open-lark = { version = "0.15", features = ["professional-suite"] }

# 阶段3：完整企业功能
open-lark = { version = "0.15", features = ["enterprise-suite"] }
```

## ⚠️ 注意事项

1. **向后兼容**：所有版本都保持 API 兼容性
2. **依赖关系**：高级功能自动包含低级功能
3. **编译时间**：功能越多，编译时间越长
4. **二进制大小**：按需选择可以减少最终文件大小

## 🆘 故障排除

### 编译错误
如果遇到编译错误，请检查：
- 功能名称拼写是否正确
- 是否启用了所有必需的依赖功能

### 功能不工作
如果某些功能不工作，请确认：
- 已启用相应的 feature flag
- 功能依赖是否满足

### 性能问题
如果遇到性能问题，请考虑：
- 减少不必要的功能模块
- 使用场景化组合而非完整套件

## 📚 更多资源

- [完整 API 文档](https://docs.rs/open-lark)
- [示例代码](https://github.com/foxzool/open-lark/tree/main/examples)
- [GitHub 仓库](https://github.com/foxzool/open-lark)
- [问题反馈](https://github.com/foxzool/open-lark/issues)